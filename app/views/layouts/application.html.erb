<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Silo Arts & Records" %></title>

    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>


    <meta name="pipe-context" content="siloarts" />

    <%- if user_signed_in? %>
      <meta name="current-user" content="<%= UserSerializer.new(current_user).to_json(root: false) %>" />
    <% end %>

    <meta name="spree-guest-api-key" content="<%= spree_guest_api_key %>" />




  </head>

  <body>
    <div id="preloader"><div id="logo"></div></div>

    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>