<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
    <meta name="pipe-context" content="siloarts" />

    <%- if user_signed_in? %>
      <meta name="current-user" content="<%= UserSerializer.new(current_user).to_json(root: false) %>" />
      <meta name="spree-api-key" content="63f0b4e13263b0932413de4e53339ae081fbe4694cda8cd3" />
    <% end %>
    
  </head>

  <body>

    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>